Lost your password? Here is a link to change it to something more memorable:

<%= "/users/#{@user.id}/reset_password/edit?token=#{@user.auth['temporary_token']['token']}" %>

If you didn't request this email be sent, just ignore it. Your password won't be changed unless you follow the link and take some action.